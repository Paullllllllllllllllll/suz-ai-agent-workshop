[project]
name = "quant-tryout"
version = "0.1.0"
description = "Workshop try-out: wages, cities, and religion in early modern Europe"
requires-python = ">=3.11"
dependencies = ["pandas>=2.2", "numpy", "matplotlib>=3.8"]

[dependency-groups]
dev = ["pytest>=8.0", "ruff>=0.6"]
